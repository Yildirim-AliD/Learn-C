#include <stdio.h>

int main() {
    char str1[] = "Hello, World!";        // Character array initialized with a string literal
    char str2[20] = "C Programming";      // Character array with fixed size and initial value

    printf("String 1: %s\n", str1);
    printf("String 2: %s\n", str2);

    return 0;
}
